#ifndef MATH_FUNFS_H
#define MATH_FUNCS_H
#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;

void primeFactorize(int& n, vector<int>& v);
void erase_duplicate(vector<int>& v);
void print(vector<int>& v);

#endif
